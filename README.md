# Pivot Design

Menu-driven calculators from the Valley Pivot Design 1.0 workbook.

```bash
npm install
npm run dev
```

Open [http://localhost:3001](http://localhost:3001). Yellow fields are inputs. Job name and field carry across every worksheet.
